import { Canvas, Meta, Story } from "@storybook/addon-docs";
import Button from "../Button/Button";
import notification from "./Notification";
import { NotificationCloseButton, NotificationWrapperExample, NotificationUpdateMessageContent } from "./Notification.example";
import Icon from "../Icon/Icon";

<Meta title="Feedback/Notification" parameters={{ backgrounds: { default: "gray" } }} />

# Notification

<Canvas parameters={{ backgrounds: { default: "gray" } }}>
  <NotificationWrapperExample label="Basic">
    <Button
      color="primary"
      onClick={() =>
        notification.open({
          message: "Notification Title",
          description: "This is the content of the notification. This is the content of the notification. This is the content of the notification.",
          onClick: () => {
            console.log("Notification Clicked!");
          },
        })
      }
    >
      Open the notification box
    </Button>
  </NotificationWrapperExample>
</Canvas>

<Canvas parameters={{ backgrounds: { default: "gray" } }}>
  <NotificationWrapperExample label="Notification with icon">
    <Button
      color="secondary"
      onClick={() =>
        notification.success({
          message: "Notification Title",
          description: "This is the content of the notification. This is the content of the notification. This is the content of the notification.",
        })
      }
    >
      Success
    </Button>
    <Button
      color="secondary"
      onClick={() =>
        notification.info({
          message: "Notification Title",
          description: "This is the content of the notification. This is the content of the notification. This is the content of the notification.",
        })
      }
    >
      Info
    </Button>
    <Button
      color="secondary"
      onClick={() =>
        notification.warning({
          message: "Notification Title",
          description: "This is the content of the notification. This is the content of the notification. This is the content of the notification.",
        })
      }
    >
      Warning
    </Button>
    <Button
      color="secondary"
      onClick={() =>
        notification.error({
          message: "Notification Title",
          description: "This is the content of the notification. This is the content of the notification. This is the content of the notification.",
        })
      }
    >
      Error
    </Button>
  </NotificationWrapperExample>
</Canvas>

<Canvas parameters={{ backgrounds: { default: "gray" } }}>
  <NotificationWrapperExample label="Customized icon">
    <Button
      color="primary"
      onClick={() =>
        notification.open({
          message: "Notification Title",
          description: "This is the content of the notification. This is the content of the notification. This is the content of the notification.",
          icon: <Icon name="home" size="text-lg" />,
        })
      }
    >
      Open the notification box
    </Button>
  </NotificationWrapperExample>
</Canvas>

<Canvas>
  <NotificationWrapperExample label="Customized container style with tailwind">
    <Button
      color="primary"
      onClick={() =>
        notification.open({
          message: "Notification Title",
          description: "I will never close automatically. This is a purposely very very long description that has many many characters and words.",
          containerClassName: "bg-primary-900",
        })
      }
    >
      Open the notification box
    </Button>
  </NotificationWrapperExample>
</Canvas>

<Canvas>
  <NotificationWrapperExample label="Customized content style with tailwind">
    <Button
      color="primary"
      onClick={() =>
        notification.open({
          message: "Notification Title",
          description: "I will never close automatically. This is a purposely very very long description that has many many characters and words.",
          className: "w-20",
        })
      }
    >
      Open the notification box
    </Button>
  </NotificationWrapperExample>
</Canvas>

<Canvas>
  <NotificationWrapperExample label="Duration after which the notification box is closed">
    <Button
      color="primary"
      onClick={() =>
        notification.open({
          message: "Notification Title",
          description: "I will never close automatically. This is a purposely very very long description that has many many characters and words.",
          duration: 0,
        })
      }
    >
      Open the notification box
    </Button>
  </NotificationWrapperExample>
</Canvas>

<Canvas parameters={{ backgrounds: { default: "gray" } }}>
  <NotificationWrapperExample label="Custom close button">
    {/* const key = `open${Date.now()}`;
    const btn = (
      <Button color="primary" onClick={() => notification.close(key)}>
        Confirm
      </Button>
    );
    return (
      <Button
        color="primary"
        onClick={() =>
          notification.open({
            message: 'Notification Title',
            description:
              'A function will be be called after the notification is closed (automatically after the "duration" time of manually).',
            btn,
            key,
            onClose: close,
          })
        }
      >
        Open the notification box
      </Button>
    ); */}
    <NotificationCloseButton />
  </NotificationWrapperExample>
</Canvas>

<Canvas>
  <Button
    color="primary"
    onClick={() => {
      const placement = "top";
      notification.info({
        message: `Notification ${placement}`,
        description: "This is the content of the notification. This is the content of the notification. This is the content of the notification.",
        placement,
      });
    }}
  >
    top
  </Button>
  <Button
    color="primary"
    onClick={() => {
      const placement = "bottom";
      notification.info({
        message: `Notification ${placement}`,
        description: "This is the content of the notification. This is the content of the notification. This is the content of the notification.",
        placement,
      });
    }}
  >
    bottom
  </Button>
  <Button
    color="primary"
    onClick={() => {
      const placement = "topLeft";
      notification.info({
        message: `Notification ${placement}`,
        description: "This is the content of the notification. This is the content of the notification. This is the content of the notification.",
        placement,
      });
    }}
  >
    topLeft
  </Button>
  <Button
    color="primary"
    onClick={() => {
      const placement = "topRight";
      notification.info({
        message: `Notification ${placement}`,
        description: "This is the content of the notification. This is the content of the notification. This is the content of the notification.",
        placement,
      });
    }}
  >
    topRight
  </Button>
  <Button
    color="primary"
    onClick={() => {
      const placement = "bottomLeft";
      notification.info({
        message: `Notification ${placement}`,
        description: "This is the content of the notification. This is the content of the notification. This is the content of the notification.",
        placement,
      });
    }}
  >
    bottomLeft
  </Button>
  <Button
    color="primary"
    onClick={() => {
      const placement = "bottomRight";
      notification.info({
        message: `Notification ${placement}`,
        description: "This is the content of the notification. This is the content of the notification. This is the content of the notification.",
        placement,
      });
    }}
  >
    bottomRight
  </Button>
</Canvas>

<Canvas>
  <NotificationWrapperExample label="Update Message Content">
    {/* 
    const NotificationUpdateMessageContent = () => {
      const key = 'updatable';
      const openNotification = () => {
        notification.open({
          key,
          message: 'Notification Title',
          description: 'description.',
        });
        setTimeout(() => {
          notification.open({
            key,
            message: 'New Title',
            description: 'New description.',
          });
        }, 1000);
      };
      return (
        <Button
          color="primary"
          onClick={openNotification}>
          Open the notification box
        </Button>
      );
    } */}
    <NotificationUpdateMessageContent />
  </NotificationWrapperExample>
</Canvas>
